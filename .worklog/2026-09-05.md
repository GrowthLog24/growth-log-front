# 작업 일지 - 2026-09-05

## 작업 목표

- [x] 라운지 방문 회원이 **명찰 QR로 셀프 출석**하도록(운영자 수동 입력 제거)
- [x] 위치가 **강남구**일 때만 출석 인정
- [x] 기존 정기모임 출결(`attendances`) 모델에 통합(신규 스키마 만들지 않기)
- [x] `play` 방침(동결 TDD·게이트 사다리)으로 검증
- [x] PR → Vercel 프리뷰로 테스트 가능하게

## 질문 및 답변 (주요 결정)

### Q1. 신원 확인 방식
- 논의: 로스터 선택 → 운영자 스캔 → 링크+GPS → **명찰 QR 재활용**으로 수렴
- **결정**: 이미 존재하는 명찰 QR(`/member/{기수}/{이름}`, 업적 페이지)에 체크인 배너를 얹는다. 신원은 URL이 담고 있어 이름 입력/선택 불필요.

### Q2. 위치 검증
- **결정**: 실내 GPS 오차 때문에 **강남구 bounding-box**(외부 API/키 의존 0). 역지오코딩 대신 좌표 범위 판정.

### Q3. 적용 범위 (정기모임 vs 그로스톡)
- 기존 시스템은 **정기모임 회차 전용**(type 없음).
- **결정**: 오늘은 **정기모임 회차만**. 그로스톡은 후속(모델 확장 필요).

### Q4. 실패 메시지
- **결정**: 실패(권한 거부/강남구 아님/오류) 시 "출석 확인에 실패했어요. 김태홍한테 문의주세요." 표시.

### Q5. 게스트
- **결정**: 게스트(비회원) 없음 — 회원(명찰)만.

### Q6. 브랜치 기준
- **결정**: 포크(origin)가 24커밋 뒤처져 원본 **upstream/main**에서 새 브랜치. (사용자 지적으로 수정)

## 작업 내역

### 흐름
운영자 `admin/meetings`에서 회차 "체크인 열기"(`checkinConfig/current`) → 회원 명찰 스캔 → 업적 페이지 배너 [출석하기] → 위치권한 → 강남구 bbox → `attendances/{meetingId}_{memberId}` status="present" 기록 → "출석 완료".

### 변경 파일
- 신규 `src/domain/checkin/geo.ts`(+spec): `isInGangnam` 강남구 bbox 판정
- 신규 `src/domain/checkin/availability.ts`(+spec): `resolveCheckinAvailability` 배너 상태 판정
- 신규 `src/infrastructure/repositories/checkinConfigRepository.ts`: `getCurrent`
- 신규 `src/infrastructure/repositories/admin/checkinConfigAdminRepository.ts`: `setCurrent`
- 수정 `attendanceRepository.ts`: `getStatus(meetingId, memberId)` 추가
- 수정 `meetingRepository.ts`: `getById` 추가
- 신규 `presentation/components/checkin/MemberCheckIn.tsx`: 위치→bbox→`saveMany(status:present)` + 상태머신
- 신규 `presentation/components/checkin/MemberCheckInGate.tsx`: 체크인 설정 실시간(클라이언트) 조회 게이트
- 수정 `app/member/[generation]/[name]/page.tsx`: `<MemberCheckInGate/>` 삽입
- 수정 `app/admin/meetings/page.tsx`: 회차별 "체크인 열기/닫기" 토글 + "체크인 받는 중" 배지
- 수정 `firebase/collections.ts`, `domain/entities/index.ts`: `checkinConfig` 컬렉션/`CheckinConfig` 타입
- 신규 `vitest.config.mts` + `package.json` test 스크립트(동결 TDD용)

### 게이트 결과
- tsc --noEmit: 0 errors ✓
- eslint(변경 파일): 0 ✓
- vitest: 13/13 ✓
- next build: 성공 ✓ (member/admin 라우트 모두 동적 렌더 `ƒ`)

## 발생 이슈

### 이슈 1: 그린필드 스키마 → 폐기
- 원인: 초기엔 `attendances`를 새로 설계(yearMonth/type/sessionDate). 그러나 upstream에 정기모임 출결 도메인이 이미 완비.
- 해결: 기존 `Attendance`(`{meetingId,memberId,generation,round,status}`, docId=`{meetingId}_{memberId}`) 100% 재사용. `attendanceAdminRepository.saveMany` 재사용.

### 이슈 2: 포크 브랜치가 stale
- 원인: origin/main이 upstream/main보다 24커밋 뒤처짐(명찰 기능 포함).
- 해결: upstream/main 기준으로 `feat/qr-attendance` 재생성.

## 교차 리뷰

### Claude 리뷰 (general-purpose 에이전트)
- HIGH: 관리자 일괄저장이 셀프 체크인을 absent로 삭제 → **수정**(저장 직전 최신 출결 병합).
- HIGH: 미인증 공개 클라이언트 쓰기(규칙 의존) → **문서화**(아래 후속).

### codex 리뷰 (codex review --base upstream/main)
- P1 서버측 권한 부재(memberId/meetingId 조작 가능) → **후속**(firebase-admin 없음·규칙 "나중" 결정, 아래).
- P1 제출 시점 체크인 창 재검증 없음 → **수정**(`MemberCheckIn` 제출 시 `checkinConfig` 재확인).
- P2 부적격 회원(비활성/상위 기수) 체크인 → **수정**(게이트에서 활성 && 가입기수 ≤ 회차기수).
- P2 집계 제외(비활성) 회차 체크인 무효 → **수정**(게이트에서 `meeting.isActive` 확인).
- P2 관리자 저장 잔여 ms 경쟁 → **후속**(현실 시나리오는 완화됨, 완전 제거는 트랜잭션 필요).

## 다음 작업 (사람/후속)

- [ ] **Firestore 보안 규칙(최우선)**: `attendances` 쓰기를 강하게 제한, `checkinConfig` 읽기공개·쓰기제한. 서버측 쓰기 경로(firebase-admin/Callable) 또는 규칙으로 신원·자격·위치 검증. (현재는 기존 규칙 그대로 진행 결정)
- [ ] 관리자 저장 잔여 경쟁 완전 제거: 트랜잭션 또는 "명시적으로 건드린 회원만 저장"(delete-by-omission 제거).
- [ ] 그로스톡 타입 지원(모델 확장 시).
- [ ] 지각 자동판정: 회차 시작시각 도입 시 present/late 자동 구분.
