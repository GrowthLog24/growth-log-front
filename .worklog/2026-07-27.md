# 작업 일지 - 2026-07-27

## 활동 카드: 무한스크롤 → 2줄 + 더 보기

사용자: "활동 카드들이 기본적으로 두줄까지만 출력되게 하고 더보기 버튼으로 추가적으로 볼 수 있게 해줘.
지금은 무한스크롤이라 화면 하단 컨텐츠가 한눈에 들어오진 않네"

### 구현
`src/presentation/components/activity/ActivityCategorySection.tsx`
- `useInfiniteScroll` + IntersectionObserver 제거 → `isExpanded` 상태로 전환
- 그리드가 반응형(1/2/3/4열)이라 "2줄"의 개수가 브레이크포인트마다 다름(2/4/6/8)
  → **인덱스별 반응형 클래스로 CSS 처리** (JS 계산 없음 = 하이드레이션 불일치 없음)
    - idx 0-1: `contents`          (항상)
    - idx 2-3: `hidden sm:contents`
    - idx 4-5: `hidden lg:contents`
    - idx 6-7: `hidden xl:contents`
- 래퍼는 `display:contents`로 레이아웃에서 제거 → 카드가 그대로 그리드 아이템이 되어
  **기존 높이 정렬과 픽셀 단위로 동일** (카드 루트가 `<article>`/`TrackedLink`로 제각각이라 필요했음)
- `trackEvent("list_expand")` 애널리틱스 유지, 펼침 시 1회 발화
- 접힘 상태 DOM은 최대 8개만 → 초기 렌더 비용도 감소
- 덤: 이 파일에 있던 `formatDate(timestamp: any)` → `number | string | null | undefined` 로 정리

### 검증
- `tsc --noEmit` exit 0 / ESLint **오류 0건** (기존 any 오류까지 해소)
- 1280px(xl): DOM 8개, 표시 8개 = 4열 × 2줄 ✓ → "더 보기" 클릭 시 11개 전체 표시, 버튼 "접기"로 전환 ✓
- 반응형 검증: 브라우저 innerWidth가 1280 고정이라 좁은 폭 실측 불가.
  다만 1280에서 `hidden sm:contents` 요소가 표시되는 것으로 반응형 변형이 `hidden`을 이김을 확인 →
  좁은 폭에서는 미디어쿼리 미적용으로 `hidden`만 남는 것이 보장됨

### 덤: 전문가 특강 나머지 3건 확인 (전날 미해결 항목)
무한스크롤이 8건에서 멈춰 못 봤던 3건이 "더 보기"로 드러남:
| 특강 | 소속 | 기수 |
|---|---|---|
| **컴퓨터 및 데이터 지식** | 한국방송통신대학교 · 정재화 교수 | 2기 | ← KNOU CS 후보 (CS 전공 주제)
| 조지아텍 입시 후기 & 비전공자 성장기 | AI Software Engineer · 정한결 | 2기 |
| 개발자가 나아가야 하는 길 | 한국방송통신대학교 · 김진욱 교수 | 1기 |

→ KNOU CS 재분류 후보 최종: `컴퓨터 및 데이터 지식`(주제·소속 모두 부합, 강력 추천) +
   방송대 교수 특강 3건(강지훈 2건, 김진욱 1건 — 주제는 Dev·커리어라 판단 필요)

## 미적용 (확인 필요)
- `ProjectsSection`(`/projects` 프로젝트 목록)은 여전히 `useInfiniteScroll` 사용 중.
  사용자 지시가 "활동 카드"였어서 범위를 넘지 않음. 동일 적용 여부 확인 필요

---

## 히어로 호버 복원 + 분야 스트립 제거 + 커뮤니티 블로그 확인

### 1. 히어로 호버 게이팅 복원 (요청)
`HeroSection.tsx` — 앞서 접근성 이유로 제거했던 호버 노출을 요청에 따라 되돌림.
- **JS 상태 없이 CSS `group-hover`로 구현** (기존은 mousemove + useState 45줄)
- 오버레이를 2겹으로 분리해 부드럽게 크로스페이드
  (단일 그라디언트의 색 정지점 전환은 스냅되기 때문)
- 힌트 "마우스를 올려보세요" 복원
- **터치 기기 대비**: `[@media(hover:none)]`에서 콘텐츠 항상 노출 + 힌트 숨김
  → 요청대로 데스크탑은 호버 노출, 모바일은 카피가 영영 안 보이는 문제 방지
- 기존과 다른 점: 원본은 히어로 **하단 30% 영역**에서만 트리거. 지금은 히어로 전체 호버

### 2. 분야 스트립 제거 (요청)
하단 `RoleMarqueeSection`이 이미 같은 내용을 18개 태그 마퀴로 출력 중임을 확인
(프론트엔드·백엔드·IT 기획·데브옵스·인공지능·게임·HR 등) → 중복이라 제거.
- `(site)/page.tsx`, `sections/index.ts`에서 참조 제거
- `sections/FieldStripSection.tsx` **파일 삭제** (참조 0건 확인 후)

### 3. 커뮤니티 블로그 — 코드 수정 불필요
"6개만 나오는데 더 있으면 더보기 달아달라" → 조사 결과 **이미 구현돼 있음**.
- `CommunityBlogSection`: `ITEMS_PER_LOAD = 6`, `hasMore = initialBlogs.length > 6`
- `communityBlogRepository.getBlogs()`는 limit 없이 전체 조회 (Wrapper가 인자 없이 호출)
- 라이브 실측: 카드 정확히 6개, 전부 `[GL 5기]` (3~7월), 버튼 미노출
→ **Firestore에 활성 블로그가 6개뿐.** 어드민에서 7개째 등록 시 버튼 자동 노출

### 검증
- `tsc --noEmit` exit 0 / ESLint 오류 0건
- 호버 전: 콘텐츠 opacity 0, 힌트 노출 ✓ / 호버 시: 카피·CTA·지표 페이드인 ✓
- 홈 섹션 순서: 히어로 → 혼자 배우면 도구 → 6개 활동 트랙 → 그로스로그 활동 → 후기 → 마퀴

---

## 프로젝트 목록도 2줄 + 더 보기 (+ 공용 컴포넌트 추출)

사용자: "프로젝트도 두줄로 해주세요."

### 공용 컴포넌트 추출 (DRY)
같은 패턴을 두 곳에 쓰게 되어 **`presentation/components/common/CollapsibleCardGrid.tsx` 신규 생성**.
- 2줄 캡(인덱스별 반응형 `contents` 클래스) + 더 보기/접기 버튼 + `list_expand` 애널리틱스를 한곳에
- 제네릭 + renderItem 렌더프롭 방식 (`items` / `getKey` / `renderItem` / `listType`)
- 향후 "2줄 → 3줄" 같은 변경이 한 파일에서 끝남

### 적용
| 파일 | 변경 |
|---|---|
| `activity/ActivityCategorySection.tsx` | 자체 구현 → `CollapsibleCardGrid` 위임 (약 45줄 감소) |
| `(site)/projects/components/ProjectsSection.tsx` | `useInfiniteScroll` 제거 → `CollapsibleCardGrid` 위임 |
| `common/index.ts` | export 1줄 추가 |

두 그리드가 `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4`로 **완전히 동일**해 그대로 재사용 가능했음.
`ProjectCard` 루트에 `h-full flex flex-col`이 있어 그리드 스트레치에 의존 → `display:contents` 래퍼가 필수였음.

### 검증
- `tsc --noEmit` exit 0 / ESLint 오류 0건
- `/projects`: DOM 8 · 표시 8(4열×2줄) → 더 보기 → 10장 전체 + "접기" ✓ (총 10개 프로젝트)
- 회귀 확인 `/dev-ai`: 전문가 특강 8+버튼, 그로스톡 8+버튼 ✓
- 회귀 확인 `/activity`: 성장일지 8, 클럽 활동 8 ✓

### 확인 필요
- `src/shared/hooks/useInfiniteScroll.ts` 가 **사용처 0건(고아)** 이 됨.
  프로젝트 기존 공용 훅이라 임의 삭제하지 않고 남겨둠. 삭제 여부 판단 필요.
